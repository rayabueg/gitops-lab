apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd-insecure.yaml

patches:
  - path: argocd-server-port-patch.yaml
    target:
      kind: Service
      name: argocd-server
